#include <stdio.h>

/**
 * main - Header
 *
 * Return: If 0 (Success)
 */

int main(void)
{
	int x = 48;

	while (x < 58)
	{
		putchar(d);
		if (x != 57)
		{
			putchar(',');
			putchar(' ');
		}
		++x;
	}
	putchar('\n');

	return (0);
}
